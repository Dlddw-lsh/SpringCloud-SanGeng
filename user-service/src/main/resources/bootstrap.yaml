spring:
  application:
    # Nacos配置中心为userService-dev.yaml
    # 指定为Data Id的前半段 默认使用的是application.yaml中的建议将里面的删除
    name: userService
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        # 配置中心服务器地址，也就是Nacos地址
        server-addr: localhost:8848
        # 文件后缀
        file-extension: yaml
        # 一定要指定
        namespace: d2e66aec-2446-4bfe-a293-36cc011e3aef