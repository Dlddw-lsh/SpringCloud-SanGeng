server:
  port: 8301

spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        cluster-name: chengdu
        # 修改为非临时实例 这里如果是非临时实例就开不了配置中心
        ephemeral: false
    loadbalancer:
      nacos:
        enabled: true